# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-here-minimum-32-characters
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/realestate

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (Optional)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all "your-*" values with actual credentials
# 3. For BETTER_AUTH_SECRET: run `openssl rand -base64 32`
# 4. Get MongoDB URI from MongoDB Atlas
# 5. Get OAuth credentials from respective provider dashboards
